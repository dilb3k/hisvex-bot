{
  "name": "hisvex-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Hisvex — Telegram bot for subscription plans, payments (Click + manual card), and expiry reminders",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "telegraf": "^4.16.3",
    "axios": "^1.13.0",
    "dotenv": "^16.4.5",
    "node-cron": "^4.2.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "tsx": "^4.19.2",
    "typescript": "^5.8.3"
  }
}
